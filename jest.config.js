const config = {
    injectGlobals: true,
    testEnvironment: 'node',
    verbose: true,
    maxWorkers: '75%',
    collectCoverage: false,
    // notify: true,
    // notifyMode: 'always',
};

export default config;