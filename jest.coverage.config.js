const config = {
    injectGlobals: true,
    testEnvironment: 'node',
    verbose: false,
    maxWorkers: '75%',
    collectCoverage: true,
    // notify: true,
    // notifyMode: 'always',
};

export default config;